'use client'

import SectionHeader from "./SectionHeader";
import { motion } from 'framer-motion';
import { fadeIn } from '../../../variants';

function Newsletter() {
  return (
    <section className="bg-newsletter bg-fixed h-[480px]">
      Newsletter
    </section>
  )
}

export default Newsletter
